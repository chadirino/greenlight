// Fallback used only when no selectedState preference exists yet (see
// lib/statePreference.ts). Only IL content exists today, so this is also
// the only real value in practice.
export const DEFAULT_STATE = "IL";
